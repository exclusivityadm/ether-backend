# Example environment values for a hosted Ether deployment.
# This file is purely illustrative; copy values into your actual environment
# management system (Render dashboard, .env, etc.) rather than committing
# secrets directly.

ENV=production
ETHER_SERVICE_NAME=ether-api
ETHER_VERSION=1.0.0
ETHER_LOG_LEVEL=INFO
ETHER_LOG_FORMAT=[%(asctime)s] [%(levelname)s] [%(name)s] %(message)s
