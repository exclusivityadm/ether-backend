import logging

logger = logging.getLogger("ether.metrics")


def run_metrics_tick() -> None:
    # Placeholder for future metrics aggregation.
    logger.debug("Metrics tick: no-op placeholder for now.")
