import logging

logger = logging.getLogger("ether.ocr")


def run_ocr_tick() -> None:
    # Placeholder for future receipt OCR queue processing.
    logger.debug("OCR tick: no-op placeholder for now.")
